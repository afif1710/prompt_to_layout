# Backend Django settings
DJANGO_SECRET_KEY=change-me-in-real-env
DJANGO_DEBUG=True

# Database (used by Django inside Docker)
DB_NAME=ai_ui_builder
DB_USER=ai_user
DB_PASSWORD=ai_password
DB_HOST=db
DB_PORT=3306

# Frontend API base (used by Vite / React)
VITE_API_URL=http://localhost:8000/api
